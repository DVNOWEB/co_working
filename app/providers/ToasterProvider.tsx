'use client'

// Toster is a toast notification library that allows us to display messages to the user
import { Toaster } from 'react-hot-toast'

const ToasterProvider = () => {
  return <Toaster />
}


export default ToasterProvider